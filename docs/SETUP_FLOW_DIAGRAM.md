# Setup Detection Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                         User Runs Command                            │
│                  (e.g., git-summary-ai analyze)                      │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
                    ┌────────────────────────┐
                    │  ensureSetupComplete() │
                    └────────────┬───────────┘
                                 │
                                 ▼
                    ┌────────────────────────┐
                    │  checkSetupStatus()    │
                    └────────────┬───────────┘
                                 │
                    ┌────────────┴────────────┐
                    │                         │
                    ▼                         ▼
        ┌───────────────────┐    ┌──────────────────────┐
        │ Check Global      │    │ Check API Keys       │
        │ Config File       │    │ (Claude/OpenAI/      │
        │ ~/.git-summary-ai │    │  GitHub/Gemini)      │
        │ /config.json      │    │                      │
        └─────────┬─────────┘    └──────────┬───────────┘
                  │                           │
                  └──────────┬────────────────┘
                             │
                 ┌───────────┴───────────┐
                 │                       │
                 ▼                       ▼
        ┌────────────────┐      ┌───────────────┐
        │ Setup Complete │      │ Setup Missing │
        └────────┬───────┘      └───────┬───────┘
                 │                      │
                 ▼                      ▼
        ┌────────────────┐      ┌──────────────────────┐
        │ ✅ Execute     │      │ ❌ Show Error        │
        │    Command     │      │    Message           │
        │                │      │                      │
        │ Continue with  │      │ • What's missing     │
        │ normal command │      │ • Available providers│
        │ logic          │      │ • Run setup command  │
        └────────────────┘      │                      │
                                │ Throw Error          │
                                │ (stop execution)     │
                                └──────────────────────┘


Command Integration Examples:
═══════════════════════════════

┌─────────────────────────────────────────────────────────────┐
│ Commands with BLOCKING setup check:                         │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  • analyze   → ensureSetupComplete('analyze')               │
│  • commit    → ensureSetupComplete('commit')                │
│  • summarize → ensureSetupComplete('summarize')             │
│  • run       → ensureSetupComplete('run')                   │
│                                                              │
│  These commands REQUIRE AI provider setup to function.      │
│  Execution stops if setup is incomplete.                    │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Commands with NON-BLOCKING warning:                         │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  • pr → warnIfSetupIncomplete()                             │
│                                                              │
│  This command can work without AI setup (only needs         │
│  GitHub token). Shows friendly warning but continues.       │
└─────────────────────────────────────────────────────────────┘


Setup Status Response Structure:
═══════════════════════════════

{
  "isSetupComplete": boolean,      // Overall status
  "hasAnyApiKey": boolean,          // At least one provider configured
  "hasGlobalConfig": boolean,       // Config file exists
  "configuredProviders": string[]   // ["claude", "openai", "copilot"]
}


Configuration Locations Checked:
═══════════════════════════════

1. Global Config:
   📄 ~/.git-summary-ai/config.json
   Contains: provider, model, targetBranch, etc.

2. API Keys (checked in order):
   
   a) Environment Variables:
      • CLAUDE_API_KEY or ANTHROPIC_API_KEY
      • OPENAI_API_KEY
      • GITHUB_TOKEN
      • GOOGLE_API_KEY or GEMINI_API_KEY
   
   b) OS Keychain (macOS/Windows):
      • Secure system storage
      • Accessed via credential manager
   
   c) Environment File:
      • ~/.git-summary-ai/.env
      • Global or local project .env
```
